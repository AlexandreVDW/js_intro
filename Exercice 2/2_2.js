let min = prompt ("Give me a min numbers");
let max = prompt ("Give me a max numbers");
let current = prompt ("Give me a current numbers");

if ((current >= min) && (current <= max)) {
  console.log(current);
}else {
    alert("You don't understand the exercice");
  }